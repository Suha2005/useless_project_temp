[project]
name = "ragebaiter"
version = "0.1.0"
description = "Awkward Opposite / Socially Inept Oracle"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Flask>=3.0.0",
    "Flask-SQLAlchemy>=3.1.0",
    "Werkzeug>=3.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
    "psycopg2-binary>=2.9.9",
]

[tool.vercel]
entrypoint = "run:app"
